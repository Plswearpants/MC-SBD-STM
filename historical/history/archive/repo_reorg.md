# Repo Reorganization Log

Append-only migration table for structure, retirement, and path changes.

| Date | Type | Component | Change | Caller Migration | Legacy Recovery |
| --- | --- | --- | --- | --- | --- |
| 2026-07-27 | Update | `repo structure docs` | Established living map triad: `docs/REPO_STRUCTURE.md`, `docs/REPO_STRUCTURE_DAG.md`, and this log. Documented four functional streams, official vs legacy entrypoints, optional real-only run-env stance, and config policy. | Linked from `docs/script_standardization.md`. No code path changes in Iteration 0. | N/A (docs only). |
| 2026-07-27 | Retirement | `legacy scripts` | Moved superseded real/synth/phase-space scripts into `historical/real/`, `historical/synthetic/`, `historical/phase_space/`. Removed stray session logs and `.asv` files. | Callers should use `scripts/real/MCSBD_block_realdata1.m`, `scripts/phase_space/run_parallel_dataset.m`, and `scripts/phase_space/visualize_dataset_metrics.m` (after Iteration 2 moves). | Restore from `historical/**` or git history. |
| 2026-07-27 | Update | `tunable config hygiene` | `update_config` no longer writes bare `*_tunable.mat` to cwd; routes to run-env `config/` or `config/runtime_tunables/`. `resolve_tunable_config` falls back to `config/` (not `examples/`). Extended `init_sbd` to addpath domain lib / solvers / vendor. Updated `.gitignore` for logs, runtime tunables, artifacts. | `utils/update_config.m`, `utils/resolve_tunable_config.m`, `Dong_func/update_config.m` (shim), `init_sbd.m`, `.gitignore`. | Unset `MC_SBD_RUN_ENV` and use pre-change `update_config` from git history for cwd writes. |
| 2026-07-27 | Update | `scripts regroup` | Moved entrypoints into `scripts/real/`, `scripts/synthetic/`, `scripts/phase_space/` (+ `real/viz/`). Updated run-env launcher generation and existing `runs/zrsite_*` launchers. Added script-level compatibility stubs and `scripts/README.md`. `init_sbd` addpaths stream script folders. | `scripts/real/*`, `scripts/synthetic/*`, `scripts/phase_space/*`, `scripts/real/create_run_environment.m`, `runs/zrsite_run_0528*/run_*.m`, `init_sbd.m`. | Use stubs under `scripts/*.m` or restore flat layout from git history. |
| 2026-07-27 | Update | `artifact roots + real viz` | Canonicalized batch I/O under `artifacts/synthetic/{datasets,parallel_runs}/` with legacy fallback to `examples/results/`. Updated `properGen_hierarchical`, `run_parallel_dataset`, and `loadMetricDataset_new` default paths. Documented real viz set in `scripts/real/viz/README.md`. | `artifacts/synthetic/README.md`, `scripts/phase_space/properGen_hierarchical.m`, `scripts/phase_space/run_parallel_dataset.m`, `lib/phase_space/loadMetricDataset_new.m`, `scripts/real/viz/README.md`. | Keep using `examples/results/` via runner fallbacks; recover old relative paths from git history. |
| 2026-07-27 | Update | `solvers vendor lib` | Moved root solver APIs into `solvers/`. Isolated `slanCM`, `imshow3D`, `mat2im` under `vendor/`. Merged `Dong_func/` into `lib/` and removed duplicate `lib/update_config.m` (utils is canonical). Left `Dong_func/README.md` redirect. Updated hardcoded addpaths in measure_snr and plotting_paper. | `solvers/*`, `vendor/*`, `lib/*`, `Dong_func/README.md`, `scripts/real/measure_snr_from_measurement.m`, `examples/data/ZrSiTe/result/plotting_paper.m`, `init_sbd.m`. | Restore prior layout from git history; `Dong_func/README.md` points to `lib/`. |
| 2026-08-24 | Fix | `cross-platform logging path` | Replaced `strcat(LOGpath,'/',LOGfile,...)` in `logUsedBlocks` with `fullfile(...)` and added explicit `fopen` failure checks. Prevents Windows path issues and silent file-open failures in all block logging happy paths. | `lib/basic/logUsedBlocks.m` | Restore prior `strcat`-based path construction from git history (not recommended on Windows). |
| 2026-08-25 | Retirement | `hist_ previous scripts` | Retired previous real/synthetic trunks in-folder with `hist_` prefix: `hist_MCSBD_block_realdata1.m`, `hist_preprocess.m`, `hist_postprocessing.m` (restored from delete), `hist_run_real_data.m` (combined raw-to-result), `hist_MCSBD_block_synthetic.m`. Official trunks are the temporary `RUN_` names: `RUN_real_preprocess.m`, `RUN_real_block.m`, `RUN_synthetic_data.m` (case-renamed from `run_synthetic_data.m`). Moved scratch `scripts/zeropads.m` to `historical/real/hist_zeropads.m`. Root stubs retargeted; `create_run_environment` launchers now generate `run_real_preprocess` / `run_real_block` / `run_hist_real_data`; existing `runs/zrsite_run_0528*` launchers retargeted to `hist_*`. | `scripts/real/*`, `scripts/synthetic/*`, `scripts/*.m` stubs, `scripts/real/create_run_environment.m`, `scripts/real/activate_run_environment.m`, `runs/zrsite_run_0528*/run_*.m`, `docs/REPO_STRUCTURE.md`, `docs/REPO_STRUCTURE_DAG.md`, `scripts/README.md` | `git mv` reverse from this commit, or run the `hist_*` files directly. Combined real path is `scripts/real/hist_run_real_data.m`. |
| 2026-08-25 | Retirement | `run-env` | Dropped run-environments as the recommended trial wrapper. Official trial record is the timestamped project folder plus `logUsedBlocks` `*_LOGfile.txt` (DATE / BLOCK / COMMENT). Helpers renamed `hist_create_run_environment.m` / `hist_activate_run_environment.m`. Users who want artifacts in a chosen directory set `params.project.root = pwd` (or pick that dir in the UI). | `scripts/real/hist_create_run_environment.m`, `scripts/real/hist_activate_run_environment.m`, `docs/REPO_STRUCTURE.md`, `docs/REPO_STRUCTURE_DAG.md` | Call the `hist_*` helpers, or restore previous filenames from git history. |
| 2026-08-25 | Update | `tunable run suffix` | Worker tunables are copied/updated as `config/runtime_tunables/{X,A}solve_config_tunable_<log.file>.mat`. Trunks call `registerTunableRun` after opening the session log; CFG01 records directory and filenames. `update_config` / `resolve_tunable_config` honor `MC_SBD_TUNABLE_ID`. Parallel in-place 3-arg worker configs are unchanged. | `utils/registerTunableRun.m`, `utils/suffixTunableFilename.m`, `utils/update_config.m`, `utils/resolve_tunable_config.m`, `scripts/real/RUN_real_preprocess.m`, `scripts/real/RUN_real_block.m`, `scripts/synthetic/RUN_synthetic_data.m` | Unset `MC_SBD_TUNABLE_ID` to restore unsuffixed `*_tunable.mat` names. |
| 2026-08-25 | Retirement | `root stubs + RUN_ prefix` | Removed `scripts/*.m` compatibility stubs and `scripts/load.ps`. Moved `hist_*` trunks/helpers to `historical/real/` and `historical/synthetic/`. Dropped the temporary `RUN_` prefix: official paths are `scripts/real/real_preprocess.m`, `scripts/real/real_block.m`, `scripts/synthetic/synthetic_data.m`. Existing `runs/zrsite_*` launchers retargeted to `historical/real/`. | `scripts/`, `historical/real/`, `historical/synthetic/`, `docs/REPO_STRUCTURE.md`, `docs/REPO_STRUCTURE_DAG.md` | Recover stubs and old names from git history. Combined real path is `historical/real/hist_run_real_data.m`. |
| 2026-08-25 | Update | `phase-space viz modular cells` | `visualize_dataset_metrics` B/V blocks are independent MATLAB sections (Run Section). Master `run_build`/`run_visualize` wrappers removed; S0 remains the recipe panel. | `scripts/phase_space/visualize_dataset_metrics.m`, `docs/REPO_STRUCTURE.md`, `history/phase_space_metrics_updates.md` | Restore S1 `if cfg.run_build` / `if cfg.run_visualize` wrapping from git history. |
| 2026-08-25 | Retirement | `root startup clutter` | Removed unused root `startup.m` (colormap/vendor path add), `function_path_analyzer.m` (one-off path dump), and `load.ps` (MATLAB print leftover). `init_sbd` now addpaths `colormap/` itself. Canonical bootstrap remains `init_sbd.m`; `bootstrap_init_sbd.m` kept as the Editor-temp-safe locator. | `init_sbd.m`, `docs/REPO_STRUCTURE.md` | Restore the three files from git history. |
| 2026-08-25 | Update | `MT-SBD → MCSBD rename` | Replaced the MT acronym with MC (multi-channel) in file names, function names, env/appdata keys (`MC_SBD_*`), and docs. `init_sbd` / `SBD` / `SBD_test_*` / existing `mcsbd_slice` hierarchy are unchanged except `mtsbd_time` → `mcsbd_time`. GitHub clone folder may still be `MT-SBD-STM`. | `solvers/MC_SBD.m`, `solvers/MCSBD_*.m`, wrappers, trunks, historical scripts, `utils/registerTunableRun.m`, living docs. New `examples/simple_MCSBD_example.m`. | Recover old `MT_SBD` / `MTSBD_*` names from git history. Env vars previously `MT_SBD_*` must be set as `MC_SBD_*`. |
| 2026-08-25 | Update | `simple MCSBD freeze/load` | Bundled 2D example dataset lives at `examples/example_data/simple_mcsbd_2d/` in `saveDataset` format. Example script: generate (optional) → WS01A freeze → LD01A load → DS01A recipe. `loadWorkspace` can load that file headlessly. | `examples/simple_MCSBD_example.m`, `lib/wrapper/loadWorkspace.m`, `.gitignore`, living docs. See `history/simple_mcsbd_example_updates.md`. | Delete the bundled pair and generate from GD01A; or restore the generate-only example from git history. |
| 2026-08-25 | Update | `simple MCSBD no session log` | Example no longer creates project folders or `*_LOGfile.txt`. Freeze/load is `data`+`params`. `logUsedBlocks` skips writes when path/file are empty. | `examples/simple_MCSBD_example.m`, `lib/basic/logUsedBlocks.m`. See `history/simple_mcsbd_example_updates.md`. | Restore the logged example path from git history. |
| 2026-08-25 | Retirement | `solvers/ trim` | Deleted dead/broken solvers; moved experimental ones to `historical/solvers/hist_*` (plus `historical/synthetic/hist_SBD_with_proximity_script.m`). Live `solvers/` is the six happy-path APIs. | `init_sbd.m`, `solvers/README.md`, historical callers listed in `history/solvers_retirement.md`. | Restore deleted files from git history; `hist_*` names remain the parked copies. |
| 2026-08-25 | Update | `README run map` | Root README now points at the 2D example (load vs generate vs freeze) and a short real / synthetic / phase-space route map. | `README.md` | Restore the intro-only README from git history. |
| 2026-08-26 | Update | `store / paper / projects` | Adopted three payload layers. `store/` is the reusable library (real processed `Y` and runs indexed by date `0528`/`0304`; synthetic single-dataset; phase-space grids). `paper/` is the manuscript freeze. `projects/` remains session logs only. Retired `artifacts/` as a canonical name; `properGen_hierarchical`, `run_parallel_dataset`, and `loadMetricDataset_new` now default to `store/phase_space/`. `real_preprocess` can promote the `*_FULL.mat` handoff into `store/real/processed/<MMDD>/`. Legacy `experimental data/` and `synthetic results/` are README redirects. | `store/**/README.md`, `paper/**/README.md`, `projects/README.md`, `scripts/phase_space/properGen_hierarchical.m`, `scripts/phase_space/run_parallel_dataset.m`, `lib/phase_space/loadMetricDataset_new.m`, `scripts/real/real_preprocess.m`, `scripts/real/real_block.m`, `lib/wrapper/loadRealPreprocessed.m`, `docs/REPO_STRUCTURE.md`, `docs/REPO_STRUCTURE_DAG.md`, `README.md`, `.gitignore` | Copy files back into the old folder names; restore `artifacts/synthetic/` paths from git history. |
| 2026-08-26 | Update | `payload move to store/paper` | Same-volume moved all reusable payloads out of `experimental data/`, `synthetic results/`, leftover `runs/zrsite_run_0528*`, and paper movies. Real: 17 `.3ds` → `store/real/raw/<sample>/`; processed `Y` by date (`0528`,`0721`,`0906`,`1027`; `0304` has no Y); 10 run `.mat` by date (`0528` 3, `0304` 4, `0721` 3) plus 0528 run-env configs. Synthetic: 36 non-parallel runs → `store/synthetic/runs/`. Phase space: 28 datasets, 17 parallel campaign dirs, `snr=3,5,7.mat`. Paper freeze is 9 hard links (8 real + 1 metrics). Movies → `paper/figures/`. Old piles are README-only. Line log: `history/payload_migration_log.txt`. | `store/**/README.md`, `paper/**/README.md`, `experimental data/README.md`, `synthetic results/README.md`, `runs/README.md`, `submission/Main Figures/plotting_paper.m` (S02 picker seeds `paper/freeze/real/`, `*_ALL.mat`), `docs/REPO_STRUCTURE.md`, `.gitignore`, `history/payload_migration.md` | Reverse the MOVE lines in `history/payload_migration_log.txt`; freeze names are extra hard links (deleting one name does not delete store bytes). |
| 2026-08-26 | Update | `store processed material prefix` | Nested `store/**/README.md` removed; only `store/README.md` remains as the map. Renamed `store/real/processed/<MMDD>/` to `store/real/processed/<Material>_<MMDD>/` (`ZrSiTe_0528`, `ZrSiTe_0304`, `LiFeAs_0721`, `PtSn4_0906`, `PtSn4_1027`). Trunks compose that folder from `cfg.io.sample` + `cfg.io.sample_date`. | `store/README.md`, `scripts/real/real_preprocess.m`, `scripts/real/real_block.m`, `lib/wrapper/loadRealPreprocessed.m`, `docs/REPO_STRUCTURE.md`, `docs/REPO_STRUCTURE_DAG.md`, `README.md`, `paper/freeze/README.md`, `paper/freeze/real/README.md` | Rename folders back to bare `MMDD`; unset `cfg.io.sample` so promote/picker use `sample_date` alone. |
| 2026-08-26 | Update | `docs + history logs` | Parked Nov 2025 session memos and the outdated `scripts/synthetic/README.md` under `historical/docs/`. Living `docs/` is the map + glossary + script standard. Update tables live only in `historical/history/` (repo-root `history/` retired). Indexes: `docs/README.md`, `historical/README.md`, `historical/history/README.md`. `plotting_paper.m` path in living docs is `paper/figures/Main Figures/`. Dropped gone `scripts/real/viz/` from the scripts map. | `docs/*`, `historical/README.md`, `historical/history/README.md`, `historical/docs/README.md`, `scripts/README.md`, `paper/README.md`, `README.md`, `.cursor/rules/history-update-log-table.mdc`, `.gitignore` | Move parked files back from `historical/docs/`; restore repo-root `history/` from git if a split log location is wanted. |
| 2026-08-26 | Update | `.gitignore data vs paper` | Ignore experiment payloads (`store/` except its README, `run entrance/inputs`, `run entrance/projects`, `run entrance/Final result`, leftover piles). Keep `*.mat` ignored globally but un-ignore `paper/**` binaries so the manuscript set is uploaded. Dropped the `examples/example_data/*.mat` track exceptions. Config/core templates stay tracked. | `.gitignore` | Restore prior exceptions from git history if example `.mat` files should be in the clone again. |
| 2026-08-26 | Fix | `init_sbd missing utils/` | `init_sbd` no longer addpaths retired repo-root `utils/` / `core/` / `vendor/` / `scripts/`. It uses `lib/` (utils first), `config/`, `3rd party/`, and `run entrance/scripts/`. | `init_sbd.m`, `docs/REPO_STRUCTURE.md`, `lib/README.md` | Restore the old addpath list from git history. |
| 2026-08-26 | Fix | `solver config path after lib/` | Core solvers no longer load `../config/` relative to `lib/core/` (that pointed at empty `lib/config/`). They use `resolve_solver_config` → repo-root `config/`. `resolve_tunable_config` / `update_config` / `registerTunableRun` use `find_repo_root` instead of two `fileparts` (which stopped at `lib/`). | `lib/utils/resolve_solver_config.m`, `lib/core/Xsolve_*.m`, `lib/core/Asolve_Manopt*.m`, `lib/core/helpers/H_function.m`, `lib/utils/resolve_tunable_config.m`, `lib/utils/update_config.m`, `lib/utils/registerTunableRun.m` | Restore relative `../config/` loads from git history. |
| 2026-08-26 | Fix | `pipeline payload targets` | Live real/synthetic/phase-space defaults now follow the settled store layout. LDoS is `store/synthetic/ldos/` via `resolve_ldos_path` (no hardcoded `examples/example_data` or Windows absolute path). Raw `.3ds` picker starts in `store/real/raw/`; Y picker uses `find_repo_root` + `store/real/processed/<Material>_<MMDD>/`; project UI defaults to `run entrance/projects/`; phase-space parallel results write under `store/phase_space/parallel_runs/<dataset_stem>/`. | `lib/utils/repo_payload_paths.m`, `lib/utils/resolve_ldos_path.m`, `lib/utils/first_existing_dir.m`, `lib/properGen_full.m`, `lib/wrapper/generateSyntheticData.m`, `lib/wrapper/loadRealDataset.m`, `lib/wrapper/loadRealPreprocessed.m`, `lib/wrapper/createProjectStructure.m`, `lib/wrapper/loadWorkspace.m`, `lib/phase_space/loadMetricDataset_new.m`, `run entrance/scripts/synthetic/synthetic_data.m`, `run entrance/scripts/real/real_preprocess.m`, `run entrance/scripts/phase_space/properGen_hierarchical.m`, `run entrance/scripts/phase_space/run_parallel_dataset.m`, `examples/simple_MCSBD_example.m`, `docs/REPO_STRUCTURE.md` | Point `LDoS_path` at an explicit file; recover old relative `example_data/` loads from git history. |
