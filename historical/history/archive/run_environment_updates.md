## Update and Deprecation Log

| Date | Type | Component | Change | Caller Migration | Legacy Recovery |
| --- | --- | --- | --- | --- | --- |
| 2026-05-28 | Update | `run environment` | Added self-contained run environment creation with dedicated `input`, `config`, `output`, and `logs` folders, plus launchers that set `MC_SBD_RUN_ENV` and run pipeline scripts from that environment. Added tunable-config resolution so solver loaders prioritize run-environment configs. | Added `scripts/create_run_environment.m`, `utils/resolve_tunable_config.m`, `utils/update_config.m`; updated `core/Xsolve_FISTA_tunable.m`, `core/Asolve_Manopt_tunable.m`, `MC_SBD.m`, and `scripts/MCSBD_block_realdata1.m`. | Recover pre-run-environment behavior from git history for the updated files, or unset `MC_SBD_RUN_ENV` to fall back to repository defaults. |
| 2026-05-28 | Update | `shared input storage` | Added shared `all_inputs` support so runtime environments can reuse large input files without per-run duplication. Runtime launcher now exposes `MC_SBD_ALL_INPUTS_DIR`; block run script can prompt and load `Y` from shared `.mat` files when not preloaded. | Updated `scripts/create_run_environment.m` and `scripts/MCSBD_block_realdata1.m` to support `SharedInputDir`, `CopyInputFiles`, input manifest writing, and runtime input auto-selection. | Recover prior per-run input-copy behavior by setting `'CopyInputFiles', true` when creating run environments, or restore prior scripts from git history. |
| 2026-05-28 | Update | `manual block execution` | Added run-environment activation-only flow so users can initialize runtime context without auto-running full scripts, then execute sections in `MCSBD_block_realdata1.m` manually with oversight. | Added `scripts/activate_run_environment.m` and runtime `activate_env.m` launcher generation in `scripts/create_run_environment.m`. | Recover prior auto-run-only launcher behavior from git history, or continue using `run_*` launchers for full-script execution. |
| 2026-05-28 | Update | `all-slice completion notification` | Added a completion hook at the end of the all-slice run block that writes a marker file, triggers local beep/popup, and optionally sends webhook/email notifications when environment variables are configured. | Updated `scripts/MCSBD_block_realdata1.m` with `notify_allslice_completion(...)` call and helper implementation. | Remove or bypass notification behavior by restoring previous script from git history, or leave optional channels unset (`MC_SBD_NOTIFY_WEBHOOK_URL`, `MC_SBD_NOTIFY_EMAIL`) to keep local-only alerts. |
| 2026-05-28 | Update | `email notification smtp env config` | Added SMTP auto-configuration from environment variables before `sendmail`, enabling unattended email notifications without manual MATLAB preference setup on each session. Supports SSL/STARTTLS with configurable server, port, sender, and credentials. | Updated `scripts/MCSBD_block_realdata1.m` completion notifier with `configure_sendmail_from_env()` and env parsing helpers. | Revert to manual MATLAB `sendmail` preferences by removing SMTP env vars, or recover previous notifier logic from git history. |
| 2026-08-25 | Update | `run-env launchers after hist_ retirement` | Run-env is optional real-data only (trial isolation / shared inputs / notify). Tunable cwd clutter is solved by `update_config` independently. New environments generate launchers for `RUN_real_preprocess`, `RUN_real_block`, and `hist_run_real_data`. Existing `runs/zrsite_run_0528*` launchers now point at `hist_MCSBD_block_realdata1.m` / `hist_run_real_data.m`. | `scripts/real/create_run_environment.m`, `scripts/real/activate_run_environment.m`, `runs/zrsite_run_0528*/run_*.m` | Restore prior launcher targets from git history. |
| 2026-08-25 | Retirement | `run-env as trial wrapper` | Run-environments are no longer recommended. Trial identity is the project folder + `*_LOGfile.txt`. Helpers renamed to `hist_create_run_environment` / `hist_activate_run_environment`. | `scripts/real/hist_create_run_environment.m`, `scripts/real/hist_activate_run_environment.m`, `docs/REPO_STRUCTURE.md` | Restore previous filenames from git history if a `runs/` workflow is needed again. |
| 2026-08-25 | Retirement | `run-env helpers to historical/` | Moved `hist_create_run_environment` / `hist_activate_run_environment` to `historical/real/`. | `historical/real/hist_create_run_environment.m`, `historical/real/hist_activate_run_environment.m` | Restore from `historical/real/` or git history. |

