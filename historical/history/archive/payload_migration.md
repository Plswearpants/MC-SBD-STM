# Payload migration (2026-08-26)

Same-volume `Move-Item` of reusable payloads into `store/` and `paper/`.
Paper freeze names are Windows hard links (same bytes as `store/`).
Line-by-line MOVE/HLINK log: [`payload_migration_log.txt`](payload_migration_log.txt).

| Date | Type | Component | Change | Caller Migration | Legacy Recovery |
| --- | --- | --- | --- | --- | --- |
| 2026-08-26 | Update | `real raw` | 17 `.3ds` files moved from `experimental data/<sample>/` into `store/real/raw/<sample>/` (Ag111 5, Cu(111) 1, LaSbTe 5, LiFeAs 1, PtSn4 4, ZrSiTe `QPImap012.3ds`). | Pickers in `real_preprocess` / `loadRealDataset` should start under `store/real/raw/`. | Reverse MOVE lines in the log. |
| 2026-08-26 | Update | `real processed` | Dated `Y` / `*_FULL.mat` (+ nearby tunables) moved to `store/real/processed/<MMDD>/`: `0528` (FULL + Y), `0721` (LiFeAs 3 + 2 tunables), `0906` (PtSn4 + 2 tunables), `1027` (PtSn4). **`0304` has no Y** — none existed. | `loadRealPreprocessed` already seeds `store/real/processed/<sample_date>/`. | Reverse MOVE lines in the log. |
| 2026-08-26 | Update | `real runs` | 10 result `.mat` by date: `0528` three `ZrSiTe0528_*_ALL.mat`; `0304` three `Chosen_*` plus `Aout_ALL.mat`; `0721` three LiFeAs result mats. Retired run-env configs → `store/real/runs/0528/run_env/`. | `plotting_paper` S02 loads from `paper/freeze/real/` (hard links of the 0528/0304 keepers). | Reverse MOVE lines; launchers remain under `runs/zrsite_run_0528*`. |
| 2026-08-26 | Update | `synthetic + phase space` | 36 non-parallel `SBD_STM_results_*.mat` → `store/synthetic/runs/`. 28 `synthetic_datasets_*.mat` → `store/phase_space/datasets/`. 17 parallel campaign directories → `store/phase_space/parallel_runs/`. `snr=3,5,7.mat` → `store/phase_space/metrics/`. | Phase-space scripts already default to `store/phase_space/` (legacy `synthetic results` still listed as fallback). | Reverse MOVE lines in the log. |
| 2026-08-26 | Update | `paper freeze` | Hard-linked 8 real `.mat` into `paper/freeze/real/` and `snr=3,5,7.mat` into `paper/freeze/phase_space/`. Movies `Aout_movie.mp4` / `qpi_movie.mp4` moved to `paper/figures/`. Assembly script stays at `submission/Main Figures/plotting_paper.m`. | S02 picker starts at freeze and lists `*_ALL.mat` only. | `fsutil hardlink list` on a freeze file; deleting one name leaves the store name. |
| 2026-08-26 | Retirement | `legacy payload piles` | `experimental data/` and `synthetic results/` are README-only. Empty leftover dirs and `load.ps` print files removed. `runs/zrsite_run_0528*` keep launcher `.m` files only. | `.gitignore` ignores contents of those two piles except their README. | Restore files from the MOVE log / git history if they were tracked. |
| 2026-08-26 | Update | `processed material prefix` | Renamed `store/real/processed/<MMDD>/` to `<Material>_<MMDD>/`. Nested store READMEs removed; map is `store/README.md` only. | `real_preprocess` / `real_block` / `loadRealPreprocessed` use `cfg.io.sample` + `cfg.io.sample_date`. | Rename folders back to bare `MMDD`. |
| 2026-08-26 | Update | `LDoS to store/synthetic/ldos` | Moved `examples/LDoS_single_defects_self=0.6_save.mat` into `store/synthetic/ldos/`. Live generators resolve that folder (then `examples/`) instead of `examples/example_data/`. | `resolve_ldos_path`; `synthetic_data.m` GD01A; `properGen_hierarchical.m`; `simple_MCSBD_example.m` GD01A | `Move-Item` the `.mat` back to `examples/`. |
