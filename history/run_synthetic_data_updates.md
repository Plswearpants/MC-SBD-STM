# Synthetic trunk script updates

| Date | Type | Component | Change | Caller Migration | Legacy Recovery |
| --- | --- | --- | --- | --- | --- |
| 2026-08-20 | Fix | `IP01A log window_type` | `mat2str` cannot format cell window specs `{'gaussian', 2.5}`. IP01A now logs with `formatWindowType`. | `scripts/synthetic/run_synthetic_data.m` | Restore `mat2str(window_type_proliferation)` from git history (will error on cell windows). |
| 2026-08-20 | Fix | `IP01A/DA01A/VR01A flat params` | Inline blocks expected `params.num_slices` after wrappers had packed `params.synGen`. Extract hierarchical params to flat at the start of IP01A, DA01A, and VR01A. | `scripts/synthetic/run_synthetic_data.m` | Keep using `params.synGen.num_slices` in those blocks, or skip `organizeParams(..., 'extract')`. |
| 2026-08-20 | Fix | `IP01A kernel stack pad` | `kernel_sizes_all` can make per-slice kernels different sizes. IP01A now center-pads each kernel to the max [H W] across slices and re-applies `proj2oblique` before stacking. | `scripts/synthetic/run_synthetic_data.m` | Restore the unpadded assignment `A1_all_matrix{k}(:,:,s) = A1_all{s,k}` from git history. |
| 2026-08-20 | Fix | `DA01A ref_results` | Stale `ref_results` struct from before the `decomposeReferenceSlice` wrapper refactor. DA01A now rebuilds `params_allslice` from flat params (name-mapped: `phase2_enable`→`phase2`, `Xsolve_method`→`Xsolve`, `kplus = ceil(kplus_factor*kernel_sizes_used)`) and reads the reference bias from `data.mcsbd_slice.extras.phase1.biter`. Only reachable now that IP01A no longer crashes on `kernel_sizes_all`. | `scripts/synthetic/run_synthetic_data.m` | Restore `params_allslice = ref_results.params` and `ref_results.extras...` from git history (requires the pre-wrapper `ref_results` variable). |
| 2026-08-20 | Fix | `findIsolatedPoints A0_used (kernel_sizes_all)` | On the `kernel_sizes_all` path `A0_used = data.A0` was a `{num_slices x num_kernels}` cell of varying sizes, so the all-slice solver's `A0_first = cell(size(A0))` was 2-D and `computeQualityMetrics` failed ("Expected A0 to be a vector"); it also mis-indexed `A0{n}`. Now packs each kernel into a `[H W num_slices]` block (center-padded per kernel to max slice size, noisy values preserved), making `A0_used` a cell vector aligned with the IP01A initial guess, `kernel_sizes_used`, and VR01A's `A0_used{k}(:,:,s)`. | `lib/wrapper/findIsolatedPoints.m`; consumed by `scripts/synthetic/run_synthetic_data.m` (DA01A, VR01A) | Restore `A0_used = data.A0` on the `kernel_sizes_all` branch from git history (reintroduces the cell-vector/mis-index errors). |
| 2026-08-20 | Fix | `DA01A initial_iteration` | All-slice call passed bare `initial_iteration`, undefined after the wrapper refactor. Added `initial_iteration = 1` to DA01A presets (inner Manopt/FISTA iterations) and to the block `clearvars`. | `scripts/synthetic/run_synthetic_data.m` | Restore reliance on an externally-defined `initial_iteration` from git history. |
| 2026-08-20 | Fix | `WS01A save_results_only` | Same refactor left stale save list (`ref_results`, `isolation_analysis`, bare `Y`/`A0`/`X0`/`num_kernels`/`num_slices`, invalid `'log.file'`). Now saves `data`, `params`, `allslice_results`, `isolation_results`, `log`. | `scripts/synthetic/run_synthetic_data.m` | Restore prior save list from git history (errors on missing variables). |
| 2026-08-21 | Update | `Synthetic VR struct migration` | Replaced VR01A direct-input plotting with `visualizeResultsSynthetic(data, params, ...)`; this new function reads all-slice outputs from `data.mcsbd_block`, performs per-slice GT comparison via `visualizeResults`, and writes reconstructed observations to `data.synGen.Y_reconstruct`. Removed stale DS01A call to `visualizeResults(Y, A0, Aout, X0, Xout, bout, extras)` that no longer matches struct-based flow. | `scripts/synthetic/run_synthetic_data.m`; `lib/visualizeResultsSynthetic.m` | Restore the old per-slice `visualizeResults(...)` loop and DS01A direct-input call from git history if legacy variable-based flow is needed. |
| 2026-08-21 | Update | `Synthetic VR interactive selector UI` | Added UI mode for synthetic visualization with dropdowns for slice number and parameter-set number, plus selectable display items (original vs reconstructed, activation similarity analysis, kernel QPI analysis). UI renders chosen items on demand and keeps `data.synGen.Y_reconstruct` updated for viewed slices; non-UI mode remains available. | `scripts/synthetic/run_synthetic_data.m`; `lib/visualizeResultsSynthetic.m` | Set `use_visualization_ui = false` in VR01A to revert to non-UI slice loop, or restore prior wrapper behavior from git history. |
| 2026-08-21 | Fix | `Synthetic VR all-slice fallback` | Some workflows/load paths reached VR01A without `data.mcsbd_block` populated even though all-slice outputs existed as `allslice_results`. `visualizeResultsSynthetic` now accepts optional `'run_data'` fallback and hydrates `data.mcsbd_block` from it; VR01A now passes `allslice_results` when available. | `scripts/synthetic/run_synthetic_data.m`; `lib/visualizeResultsSynthetic.m` | Remove `'run_data'` plumbing and require `data.mcsbd_block` only (older strict behavior) by restoring from git history. |
| 2026-08-21 | Fix | `Activation panel title wording` | Corrected activation-analysis figure `sgtitle` from "Kernel Similarity Analysis" to "Activation Similarity Analysis" so UI/figure labeling matches `X` semantics (activation maps, not kernels). | `lib/computeActivationSimilarity.m` | Restore prior `sgtitle` text from git history if the older wording is intentionally desired. |
| 2026-08-21 | Update | `Synthetic VR dashboard layout` | Replaced the minimal selector UI with a dashboard-style `uifigure`: persistent control panel (slice/parameter-set/kernel selectors, display toggles, update/save/done buttons) and tabbed visualization panes for reconstruction, activation, and kernel QPI with live in-place updates. | `lib/visualizeResultsSynthetic.m` | Restore prior selector-only UI implementation from git history if lightweight popup behavior is preferred. |
| 2026-08-21 | Fix | `DA01A all-slice kernel count mismatch` | `MCSBD_synthetic_all_slice` indexes `lambda1(n)` and `xinit{n}` by kernel count derived from `kernel_sizes_used`; DA01A previously built vectors/cells using `params.num_kernels`, which could disagree after load/refactor and trigger index errors. DA01A now uses `num_kernels_allslice = size(kernel_sizes_used,1)` for display/xinit loops and normalizes `lambda1`/`lambda2` to that length (scalar broadcast or strict length check). | `scripts/synthetic/run_synthetic_data.m` | Revert to `params.num_kernels`-driven all-slice setup from git history (reintroduces indexing risk when counts diverge). |
| 2026-08-25 | Fix | `one-slice Y is 2-D` | MATLAB drops a trailing singleton, so `num_slices=1` yields HxW rather than HxWx1. `d3gridDisplay` now accepts a 2-D image as a one-slice stack; `properGen_full` uses `imagesc` for that case instead of the 3-D slider. | `lib/d3gridDisplay.m`, `lib/properGen_full.m`; unblocks `examples/simple_MCSBD_example.m` | Restore the `ndims==3` check and unconditional `d3gridDisplay(Y)` from git history (1-slice generation will error again). |
| 2026-08-25 | Fix | `decomposeReferenceSlice missing A` | Wrapper stored `X`/`b`/`extras` but never `data.A = A_ref`, so `mcsbd_slice.A` was absent after `organizeData` write/extract. Header already documented that field. | `lib/wrapper/decomposeReferenceSlice.m`; `examples/simple_MCSBD_example.m` VR01A | Restore the store-without-`A` block from git history (viz that reads `data.A` will fail). |
| 2026-08-25 | Update | `loadWorkspace headless` | Optional `'interactive'`, `'new_log_path'`, and `'new_log_file'` skip overwrite/log-directory prompts so a scripted load of a `saveDataset` workspace can open a new session log. Default remains UI. | `lib/wrapper/loadWorkspace.m`; `examples/simple_MCSBD_example.m` LD01A | Omit the new name-value pairs (interactive stays true). |
