# Repo Reorganization Log

Append-only migration table for structure, retirement, and path changes.

| Date | Type | Component | Change | Caller Migration | Legacy Recovery |
| --- | --- | --- | --- | --- | --- |
| 2026-07-27 | Update | `repo structure docs` | Established living map triad: `docs/REPO_STRUCTURE.md`, `docs/REPO_STRUCTURE_DAG.md`, and this log. Documented four functional streams, official vs legacy entrypoints, optional real-only run-env stance, and config policy. | Linked from `docs/script_standardization.md`. No code path changes in Iteration 0. | N/A (docs only). |
| 2026-07-27 | Retirement | `legacy scripts` | Moved superseded real/synth/phase-space scripts into `historical/real/`, `historical/synthetic/`, `historical/phase_space/`. Removed stray session logs and `.asv` files. | Callers should use `scripts/real/MTSBD_block_realdata1.m`, `scripts/phase_space/run_parallel_dataset.m`, and `scripts/phase_space/visualize_dataset_metrics.m` (after Iteration 2 moves). | Restore from `historical/**` or git history. |
| 2026-07-27 | Update | `tunable config hygiene` | `update_config` no longer writes bare `*_tunable.mat` to cwd; routes to run-env `config/` or `config/runtime_tunables/`. `resolve_tunable_config` falls back to `config/` (not `examples/`). Extended `init_sbd` to addpath domain lib / solvers / vendor. Updated `.gitignore` for logs, runtime tunables, artifacts. | `utils/update_config.m`, `utils/resolve_tunable_config.m`, `Dong_func/update_config.m` (shim), `init_sbd.m`, `.gitignore`. | Unset `MT_SBD_RUN_ENV` and use pre-change `update_config` from git history for cwd writes. |
| 2026-07-27 | Update | `scripts regroup` | Moved entrypoints into `scripts/real/`, `scripts/synthetic/`, `scripts/phase_space/` (+ `real/viz/`). Updated run-env launcher generation and existing `runs/zrsite_*` launchers. Added script-level compatibility stubs and `scripts/README.md`. `init_sbd` addpaths stream script folders. | `scripts/real/*`, `scripts/synthetic/*`, `scripts/phase_space/*`, `scripts/real/create_run_environment.m`, `runs/zrsite_run_0528*/run_*.m`, `init_sbd.m`. | Use stubs under `scripts/*.m` or restore flat layout from git history. |
| 2026-07-27 | Update | `artifact roots + real viz` | Canonicalized batch I/O under `artifacts/synthetic/{datasets,parallel_runs}/` with legacy fallback to `examples/results/`. Updated `properGen_hierarchical`, `run_parallel_dataset`, and `loadMetricDataset_new` default paths. Documented real viz set in `scripts/real/viz/README.md`. | `artifacts/synthetic/README.md`, `scripts/phase_space/properGen_hierarchical.m`, `scripts/phase_space/run_parallel_dataset.m`, `lib/phase_space/loadMetricDataset_new.m`, `scripts/real/viz/README.md`. | Keep using `examples/results/` via runner fallbacks; recover old relative paths from git history. |
| 2026-07-27 | Update | `solvers vendor lib` | Moved root solver APIs into `solvers/`. Isolated `slanCM`, `imshow3D`, `mat2im` under `vendor/`. Merged `Dong_func/` into `lib/` and removed duplicate `lib/update_config.m` (utils is canonical). Left `Dong_func/README.md` redirect. Updated hardcoded addpaths in measure_snr and plotting_paper. | `solvers/*`, `vendor/*`, `lib/*`, `Dong_func/README.md`, `scripts/real/measure_snr_from_measurement.m`, `examples/data/ZrSiTe/result/plotting_paper.m`, `init_sbd.m`. | Restore prior layout from git history; `Dong_func/README.md` points to `lib/`. |
