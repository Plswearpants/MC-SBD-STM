## Update and Deprecation Log

| Date | Type | Component | Change | Caller Migration | Legacy Recovery |
| --- | --- | --- | --- | --- | --- |
| 2026-06-15 | Update | `phase-space denoising metric` | Added gated denoising metric computation based on `sigma_in/sigma_out` from `A0`/`Aout`/`A0_noiseless` using `estimateKernelGaussianNoiseVsGT`, with default activation-similarity gate `>=0.95`; below-threshold slots are `NaN`. Added new heatmap metric type `denoising` to per-SNR interpolated phase-space plotting. | Added `Dong_func/phase_space/build_denoising_sigma_metrics.m`; updated `Dong_func/phase_space/metrics2heat_by_snr_interpolated.m` to support `metric_type='denoising'`; updated `examples/visualize_dataset_metrics.m` block after `%% add denoising into metrics` to build and plot denoising phase space. | Revert these files to prior commit via git history to remove denoising metric fields/plots and restore previous metric-type options. |
| 2026-06-15 | Update | `phase-space denoising alignment toggle` | Added explicit alignment toggle for denoising metric computation. New option `enableAlignment` defaults to `false`, and when disabled forces `kplus=[0,0]` (no shift search). | Updated `Dong_func/phase_space/build_denoising_sigma_metrics.m` to parse `enableAlignment`; updated `examples/visualize_dataset_metrics.m` denoising block to pass `'enableAlignment', false` explicitly. | Re-enable prior shift-search behavior by calling `build_denoising_sigma_metrics(..., 'enableAlignment', true, 'kplus', [dy dx])` or restoring prior file versions from git history. |
| 2026-06-15 | Update | `phase-space Nobs metric` | Added a standard `Nobs` metric field and integrated it into per-SNR interpolated phase-space plotting so `Nobs` can be plotted in the same format as other metrics. | Added `Dong_func/phase_space/build_nobs_metric.m`; updated `Dong_func/phase_space/metrics2heat_by_snr_interpolated.m` to support `metric_type='nobs'`; updated `examples/visualize_dataset_metrics.m` to call `build_nobs_metric(metrics)` and `metrics2heat_by_snr_interpolated(metrics, 'nobs', axis3_mode, ...)`. | Revert these files from git history to remove the `Nobs` metric type and return to custom/manual `Nobs` plotting blocks. |
| 2026-06-15 | Update | `phase-space Nobs source simplification` | Simplified `nobs` heatspace plotting to use existing `metrics.Nobs_at_axis3` directly (with `metrics.Nobs` still accepted if present), removing the required pre-build step in example workflow. | Updated `Dong_func/phase_space/metrics2heat_by_snr_interpolated.m` `metric_type='nobs'` source selection; updated `examples/visualize_dataset_metrics.m` to call `metrics2heat_by_snr_interpolated(..., 'nobs', ...)` directly without `build_nobs_metric`. | Restore prior explicit-prebuild workflow by reintroducing `build_nobs_metric(metrics)` call in `examples/visualize_dataset_metrics.m` and stricter field requirement in `metrics2heat_by_snr_interpolated.m`. |
| 2026-06-15 | Update | `Nobs_at_axis3 source correction` | Changed `loadMetricDataset_new` to derive `metrics.Nobs_at_axis3` from loaded `dataset_X0` geometry (activation map size) instead of relying only on dataset parameter `N_obs`; added fallback to parameter only when `X0` is unavailable, plus mismatch diagnostics and summary counts. | Updated `Dong_func/phase_space/loadMetricDataset_new.m` to store `Nobs_param_at_axis3`, infer `Nobs_at_axis3` via `dataset_X0`, and report parameter-vs-X0 mismatches and axis-slot collision context. | Restore prior parameter-only assignment by reverting `Dong_func/phase_space/loadMetricDataset_new.m` from git history. |
| 2026-06-15 | Update | `Nobs definition from X0 occurrences` | Updated `Nobs_at_axis3` derivation to count nonzero occurrences in `X0` per kernel type (instead of using activation-map size). Stored per-kernel occurrence counts and set scalar `Nobs_at_axis3` to the per-slot mean across kernel types. | Updated `Dong_func/phase_space/loadMetricDataset_new.m` helper `infer_nobs_from_x0` and added `Nobs_per_kernel_at_axis3` for audit/analysis. | Revert `Dong_func/phase_space/loadMetricDataset_new.m` from git history to restore prior map-size-based `Nobs` derivation. |
| 2026-06-15 | Update | `visualize_dataset_metrics Nobs recompute block` | Added an explicit script block to recompute `Nobs` from nonzero `X0` occurrences per kernel for each loaded slot, then assign `metrics.Nobs_at_axis3`, `metrics.Nobs_per_kernel_at_axis3`, and `metrics.Nobs` before downstream plotting. | Updated `examples/visualize_dataset_metrics.m` after metric loading/build steps with a loop-based `X0` occurrence recomputation block. | Remove or revert the added block in `examples/visualize_dataset_metrics.m` to return to loader-only `Nobs` assignment. |
| 2026-06-15 | Update | `Nobs vs denoising scatter plots` | Added per-SNR scatter visualization of `Nobs` (x-axis) vs denoising ratio `sigma_in/sigma_out` (y-axis), with one subplot per SNR and invalid values omitted. | Updated `examples/visualize_dataset_metrics.m` after denoising/Nobs heatmaps to add `Nobs_at_axis3` vs `denoising_sigma_ratio_final` scatter block. | Remove the added scatter block from `examples/visualize_dataset_metrics.m` or restore file from git history. |
| 2026-06-15 | Update | `Nobs-vs-denoising scatter modes` | Updated scatter plotting to support two analysis modes: `mean` (slot-level averages) and `per_kernel` (kernel-level points from highest-resolution metric fields). Both modes are plotted per SNR with invalid entries omitted. | Updated `examples/visualize_dataset_metrics.m` scatter section to iterate over `mean` and `per_kernel` modes using `Nobs_per_kernel_at_axis3` and `denoising_sigma_ratio_per_kernel` for kernel-resolved plotting. | Restore prior single-mode scatter behavior by reverting `examples/visualize_dataset_metrics.m` from git history. |
| 2026-06-15 | Update | `side-ratio conditioned per-kernel scatter` | Added per-kernel `Nobs` vs denoising scatter plots grouped by side-length ratio (one subplot per ratio), with kernel-specific colors and invalid entries omitted. | Updated `examples/visualize_dataset_metrics.m` to aggregate points across SNR/theta/repetition at fixed side-ratio bins using `Nobs_per_kernel_at_axis3` and `denoising_sigma_ratio_per_kernel`. | Remove the side-ratio scatter block from `examples/visualize_dataset_metrics.m` or restore from git history. |
| 2026-06-15 | Update | `SNR subset for scatter sections` | Restricted scatter plotting to selected SNR targets `[3, 5, 7]` (nearest available SNR grid points). Side-ratio conditioned scatter now generates one figure per selected SNR. | Updated `examples/visualize_dataset_metrics.m` in both scatter sections to map target SNR values to nearest indices before plotting. | Revert SNR subset logic in `examples/visualize_dataset_metrics.m` to restore all-SNR plotting. |
| 2026-06-15 | Update | `denoising metric definition change` | Changed denoising metric from ratio `\sigma_{in}/\sigma_{out}` to normalized reduction `(\sigma_{in}-\sigma_{out})/\sigma_{in}` at build time, preserving per-kernel and aggregated storage fields. | Updated `Dong_func/phase_space/build_denoising_sigma_metrics.m` metric formula and comments; updated labels in `Dong_func/phase_space/metrics2heat_by_snr_interpolated.m` and `examples/visualize_dataset_metrics.m`. | Revert affected files from git history to restore previous ratio definition. |
| 2026-06-15 | Update | `denoising metric standardization` | Standardized detailed-point and standalone sigma-analysis reporting to normalized denoising `(\sigma_{in}-\sigma_{out})/\sigma_{in}` and aligned zero-reference plotting behavior. | Updated `Dong_func/phase_space/visualize_heatspace_details_properGen.m` and `scripts/analyze_kernel_noise_vs_gt.m` to compute/print/plot normalized denoising consistently; added backward-compatible alias fields/variables for prior naming. | Revert these files from git history to restore prior improvement-factor (`sigma_{in}/sigma_{out}`) reporting. |
| 2026-06-15 | Update | `visualize_dataset_metrics reorganization` | Reorganized `visualize_dataset_metrics` into explicit build and visualization parts with helper functions for readability, while preserving existing plots/flows and keeping denoising/Nobs/NOR-LOO integration. Also aligned denoising scatter reference baselines to zero for normalized denoising. | Updated `examples/visualize_dataset_metrics.m` by introducing `build_metrics_part`, `visualize_metrics_part`, reusable scatter helpers, centralized configuration, and preserved optional combined/legacy views. | Restore previous single-block script style by reverting `examples/visualize_dataset_metrics.m` from git history. |
| 2026-06-15 | Update | `block outline modular navigation` | Split both build and visualization pipelines into clearly named modular blocks with concise block wrappers (`B1`-`B6`, `V1`-`V8`) and added a top-of-file outline to improve editor navigation and maintainability. | Updated `examples/visualize_dataset_metrics.m` to add top outline comments, block-level wrappers, and section naming without changing core metric/plot logic. | Revert `examples/visualize_dataset_metrics.m` from git history to restore prior organization without block-outline wrappers. |
| 2026-06-15 | Update | `sigma fit-goodness visualization utility` | Added a standalone utility to visualize and summarize sigma fit confidence using existing estimator diagnostics (`r2`, `mre`, `rmse`, `sigma`) with configurable thresholds and pass-mask logic. Supports input as raw `details` or parent structs carrying `kernel_sigma_details`. | Added `Dong_func/visualizeSigmaFitGoodness.m` with side selection (`in/out/both`), threshold options, heatmaps/histograms, and textual confidence summaries. | Remove or revert `Dong_func/visualizeSigmaFitGoodness.m` from git history to restore previous manual fit-quality inspection workflow. |
| 2026-06-15 | Update | `denoising fit-quality field` | Extended denoising-metric build to record fit goodness as median `R^2` per dataset slot, derived from input/output sigma-fit diagnostics. Also stored per-kernel and per-slice `R^2` tensors for downstream analysis. | Updated `Dong_func/phase_space/build_denoising_sigma_metrics.m` to request estimator details, merge in/out `R^2`, and add `denoising_fit_r2_final`, `denoising_fit_r2_per_kernel`, and `denoising_fit_r2_matrix`. | Revert `Dong_func/phase_space/build_denoising_sigma_metrics.m` from git history to remove added fit-quality fields and restore prior denoising-only outputs. |
| 2026-06-15 | Update | `denoising fit R^2 heatmap` | Added an interpolated per-SNR heatmap metric type for denoising fit `R^2` so fit quality can be visualized in the same phase-space format as other metrics. | Updated `Dong_func/phase_space/metrics2heat_by_snr_interpolated.m` to support `metric_type='denoising_r2'`; updated `examples/visualize_dataset_metrics.m` V4 block to plot the new heatmap. | Revert these files from git history to remove the `denoising_r2` heatmap option and call site. |
| 2026-06-15 | Update | `gaussian-fit confidence R^2` | Clarified fit-confidence semantics by adding Gaussian residual QQ-linearity `R^2` (`gauss_r2`) in sigma estimator details and using it as the primary denoising fit-quality signal when building metrics. Also made fit-goodness visualization default to Gaussian-fit `R^2`. | Updated `Dong_func/estimateKernelGaussianNoiseVsGT.m` to compute/store `gauss_r2` and `gauss_rmse`; updated `Dong_func/phase_space/build_denoising_sigma_metrics.m` to consume Gaussian-fit `R^2` (with model-`R^2` fallback); updated `Dong_func/visualizeSigmaFitGoodness.m` with `r2Field` option defaulting to `gaussian`; updated `Dong_func/phase_space/metrics2heat_by_snr_interpolated.m` label for `denoising_r2`. | Revert these files from git history to restore prior model-fit `R^2`-only confidence fields and labels. |
| 2026-06-15 | Update | `remove legacy model-fit goodness path` | Removed the previous model-fit (`a*GT+b`) `R^2` path from sigma-confidence workflows so goodness-of-fit now strictly refers to Gaussian residual-fit quality (`gauss_r2`) only. | Updated `Dong_func/phase_space/build_denoising_sigma_metrics.m` to drop model-`R^2` fallback in fit-confidence extraction; updated `Dong_func/visualizeSigmaFitGoodness.m` to remove `r2Field` option and require/display Gaussian-fit `R^2` only. | Revert these files from git history to restore the optional model-`R^2` goodness route. |
| 2026-06-15 | Update | `qq/chi-square gaussian goodness` | Added explicit Gaussian goodness-of-fit statistics for residuals based on QQ diagnostics and chi-square GOF, then switched denoising fit-confidence aggregation/heatmaps to chi-square (`\chi^2`) and p-value reporting. | Updated `Dong_func/estimateKernelGaussianNoiseVsGT.m` to store `gauss_chi2`, `gauss_pvalue`, `gauss_dof`, `gauss_reject` (plus existing QQ metrics); updated `Dong_func/phase_space/build_denoising_sigma_metrics.m` to store `denoising_fit_chi2_*` and `denoising_fit_pvalue_*`; updated `Dong_func/phase_space/metrics2heat_by_snr_interpolated.m` with `metric_type='denoising_chi2'` and `'denoising_pvalue'`; updated `examples/visualize_dataset_metrics.m` V4 block to plot both; updated `Dong_func/visualizeSigmaFitGoodness.m` confidence logic to use chi-square p-value threshold. | Revert these files from git history to restore prior R^2-centered Gaussian-fit confidence workflow. |
| 2026-06-16 | Update | `denoising ratio metric restoration` | Restored denoising score to ratio form `\sigma_{in}/\sigma_{out}` (to match stacking-law interpretation where ideal gain scales as `\sqrt{Nobs}`), and updated downstream plot baselines/labels accordingly. Kept Gaussian GOF chi-square/p-value confidence metrics unchanged. | Updated `Dong_func/phase_space/build_denoising_sigma_metrics.m` ratio formula and helper division logic; updated `Dong_func/phase_space/metrics2heat_by_snr_interpolated.m`, `examples/visualize_dataset_metrics.m`, and `Dong_func/phase_space/visualize_heatspace_details_properGen.m` labels/reference lines to ratio convention. | Revert these files from git history to restore normalized-difference denoising metric convention. |
| 2026-06-16 | Update | `sigma_in slope hypothesis scatter` | Added explicit stacking-law diagnostic by plotting `1/\sqrt{Nobs}` vs `\sigma_{out}` and overlaying dashed hypothesis lines `y=\sigma_{in}x` (slope estimated as median `\sigma_{in}` per subplot). Also exposed `\sigma_{in}`/`\sigma_{out}` tensors in denoising metrics for direct plotting. | Updated `Dong_func/phase_space/build_denoising_sigma_metrics.m` to store `denoising_sigma_in_*` and `denoising_sigma_out_*`; updated `examples/visualize_dataset_metrics.m` V5 scatter logic/labels to `1/sqrt(Nobs)` vs `sigma_out` with hypothesis overlays. | Revert these files from git history to restore prior Nobs-vs-ratio scatter style and remove direct sigma-in/out storage fields. |
| 2026-06-16 | Update | `build/plot control panel toggles` | Added concise logical control panel toggles so users can choose which build blocks and visualization blocks run without commenting code. | Updated `examples/visualize_dataset_metrics.m` with `cfg.run_build`, `cfg.run_visualize`, `cfg.build.*` (B1..B6), and `cfg.plot.*` (V1..V8), and conditional dispatch in pipeline runners. | Revert `examples/visualize_dataset_metrics.m` from git history to restore always-on block execution behavior. |
| 2026-06-16 | Update | `upfront documented control panel` | Moved practical control to the top-of-file config section and documented each true/false toggle with concise user-facing meaning so block selection is understandable without reading function internals. | Updated `examples/visualize_dataset_metrics.m` S0 section to explicitly set/override all build and plot booleans plus key run parameters, each with inline descriptions. | Revert `examples/visualize_dataset_metrics.m` from git history to restore defaults-only control style. |
| 2026-06-16 | Update | `unified inspector and B3 default cleanup` | Consolidated interactive dataset navigation into one unified inspector (click + manual fields + Enter/Plot) and removed overlapping dual-inspector invocation from the default workflow. Also set B3 (normalized KS build) off by default since standard KS is already available from loaded metrics. | Added `Dong_func/phase_space/inspect_dataset_heatmap_unified.m`; updated `examples/visualize_dataset_metrics.m` V3 block to call unified inspector only; updated top config defaults/comments to set `build.normalized_kernel_similarity=false` and add inspector controls. | Revert these files from git history to restore the separate click/manual inspector flow and previous B3 default behavior. |
| 2026-06-16 | Update | `visualize executable-block restoration` | Restored `visualize_dataset_metrics.m` to script-first executable sections for B1..B6 and V1..V8, replacing function-wrapped orchestration while preserving current behaviors and outputs. | Updated `examples/visualize_dataset_metrics.m` to execute build/plot blocks directly under `cfg` guards, keep unified inspector (`inspect_dataset_heatmap_unified`) in V3, preserve B3 default off, and retain denoising/Nobs scatter diagnostics with reusable utilities only. | Revert `examples/visualize_dataset_metrics.m` from git history to restore the function-wrapper orchestration style. |
| 2026-06-16 | Update | `unified inspector deep cleanup` | Stabilized and cleaned `inspect_dataset_heatmap_unified` internals: fixed parser-breaking token, made side-ratio-to-axis mapping SNR-aware for manual selection, normalized multiline function signatures for compatibility, and hardened residual plotting aggregation. | Updated `Dong_func/phase_space/inspect_dataset_heatmap_unified.m` callback mapping flow (`map_axis_index` now accepts selected SNR index), fixed malformed `end` in residual helper, simplified residual value aggregation, and standardized local function signatures. | Revert `Dong_func/phase_space/inspect_dataset_heatmap_unified.m` from git history to restore the previous implementation. |
| 2026-06-16 | Update | `xcorr-based kernel shift fit` | Switched kernel shift estimation in sigma denoising fit to normalized cross-correlation alignment (same family as activation-map alignment) without Gaussian broadening, while keeping gain+bias residual sigma estimation unchanged. | Updated `Dong_func/estimateKernelGaussianNoiseVsGT.m` to replace minrelerr-style shift search with bounded `normxcorr2` peak selection within `kplus`, then compute aligned residual fit metrics from that shift. | Revert `Dong_func/estimateKernelGaussianNoiseVsGT.m` from git history to restore prior minrelerr-style shift search. |
| 2026-06-16 | Update | `sigma-fit parameter/function cleanup` | Removed legacy alignment plumbing no longer needed after xcorr migration: dropped `shiftMode`/zeropad mode handling and deleted the unused zero-pad shift helper from sigma fitting, while preserving current outputs (`mre`, `rmse`, GOF fields, aligned stacks). | Updated `Dong_func/estimateKernelGaussianNoiseVsGT.m` option parser/docs/details to remove `shiftMode`; removed conditional zeropad branch and removed local function `apply_integer_shift_zeropad`. | Revert `Dong_func/estimateKernelGaussianNoiseVsGT.m` from git history to restore shift-mode/zeropad support. |
| 2026-06-16 | Update | `v3 shift print diagnostics` | Added explicit per-kernel shift reporting in V3 sigma diagnostics so users can verify whether alignment is actually moving kernels. Prints input/output shift median and dy/dx ranges across evaluated energies. | Updated `Dong_func/phase_space/inspect_dataset_heatmap_unified.m` (`print_sigma_diagnostics`) to call new local helpers that summarize `details.in.shift` and `details.out.shift` by kernel. | Revert `Dong_func/phase_space/inspect_dataset_heatmap_unified.m` from git history to remove shift summary printing. |
| 2026-06-16 | Update | `build-phase residual kernel storage` | Extended denoising build phase to optionally store mean residual kernels (input and output) per dataset slot using the same shift+gain+bias fit details used for sigma metrics. Added control-panel toggle for memory-aware enabling. | Updated `Dong_func/phase_space/build_denoising_sigma_metrics.m` with new option `storeResidualKernels` and output fields `denoising_residual_in_maps`/`denoising_residual_out_maps`; updated `examples/visualize_dataset_metrics.m` to expose `cfg.store_residual_kernels` and pass it into B5. | Revert these files from git history to remove residual-kernel storage fields/options and restore prior denoising build behavior. |
| 2026-06-16 | Update | `remove denoising_fit outputs from builder` | Removed all `denoising_fit_*` chi-square/p-value outputs from the denoising builder so B5 now stores only ratio/sigma and optional residual-kernel maps. | Updated `Dong_func/phase_space/build_denoising_sigma_metrics.m` to delete fit-quality tensor allocations, extraction/merge logic, output assignments, and related helper functions. | Revert `Dong_func/phase_space/build_denoising_sigma_metrics.m` from git history to restore `denoising_fit_*` outputs. |
| 2026-06-16 | Update | `v3 residual source prefers metrics` | Updated V3 inspector residual plotting to use prebuilt residual kernels from metrics when available, with automatic fallback to on-the-fly recomputation only when residual maps are missing for the selected slot. | Updated `Dong_func/phase_space/inspect_dataset_heatmap_unified.m` to read `denoising_residual_in_maps`/`denoising_residual_out_maps` for selected indices and route plotting through a shared residual-map renderer. | Revert `Dong_func/phase_space/inspect_dataset_heatmap_unified.m` from git history to restore always-recompute residual plotting behavior. |
| 2026-06-16 | Update | `v3 kernel triplet snapshot` | Restored explicit kernel-view panel in V3 by plotting input noisy kernels together with GT and output kernels for the selected dataset point, matching pre-unification inspection expectations. | Updated `Dong_func/phase_space/inspect_dataset_heatmap_unified.m` to render a new tiled snapshot figure (`input noisy / GT / output`) per kernel at the middle evaluated energy slice. | Revert `Dong_func/phase_space/inspect_dataset_heatmap_unified.m` from git history to remove the added V3 kernel snapshot figure. |
| 2026-06-16 | Update | `v3 bone colormap policy` | Enforced `bone` colormap across kernel/residual and non-activation observation figures in V3 so visual style is consistent for input/GT/output kernels and residual views, while leaving activation analyses unchanged. | Updated `Dong_func/phase_space/inspect_dataset_heatmap_unified.m` to use `bone(256)` in kernel triplet/residual renderers and to post-process new non-activation figures created by `visualizeResults` with `bone`. | Revert `Dong_func/phase_space/inspect_dataset_heatmap_unified.m` from git history to restore prior mixed colormap behavior. |
| 2026-06-16 | Update | `v5 scatter reverted to ratio-vs-sqrtNobs` | Reverted V5 diagnostics from `1/sqrt(Nobs)` vs `sigma_out` back to `sqrt(Nobs)` vs denoising ratio `sigma_in/sigma_out` for both mean and per-kernel modes, and removed hypothesis-line fitting from this section. | Updated `examples/visualize_dataset_metrics.m` V5 scatter data sources/labels/titles to use `denoising_sigma_ratio_*` fields and `sqrt(Nobs)` x-transform; simplified per-kernel scatter helpers by removing slope/hypothesis utilities. | Revert `examples/visualize_dataset_metrics.m` from git history to restore the previous `1/sqrt(Nobs)` vs `sigma_out` hypothesis-line diagnostics. |
| 2026-06-16 | Update | `v5 side-ratio cutoff and wide tiling` | Added side-length-ratio boundary control for V5 per-side-ratio scatter (`<= cutoff`, default `0.23`) and updated subplot tiling to prefer `a x b` layouts with `a < b` and near-balanced factors for clearer presentation (e.g., `2x4` for 8 panels). | Updated `examples/visualize_dataset_metrics.m` with `cfg.scatter_side_ratio_cutoff`, side-ratio filtering in `plot_nobs_vs_denoising_by_side_ratio`, and a new `choose_subplot_layout_wide` helper used by that plot block. | Revert `examples/visualize_dataset_metrics.m` from git history to restore all-side-ratio plotting and previous generic layout behavior. |
| 2026-06-16 | Update | `v5 mean-mode side-ratio colormap` | Enhanced V5 mean-mode scatter to color points by side-length ratio using a linear colormap and colorbar, improving readability of mixed side-ratio points within each SNR subplot. | Updated `examples/visualize_dataset_metrics.m` mean-mode path in `plot_nobs_vs_denoising_scatter` to use side-ratio color coding (`parula`) and added helpers to build side-ratio vectors aligned with flattened slot points. | Revert `examples/visualize_dataset_metrics.m` from git history to restore single-color mean-mode scatter points. |
| 2026-06-16 | Update | `plot-format handle section` | Added a dedicated top-level plot-format handle section so V5 style controls (font family/sizes and marker sizes) are centrally editable from config, then applied consistently to axes labels/titles/colorbar and supertitles. | Updated `examples/visualize_dataset_metrics.m` with `cfg.plot_format` controls in S0 and `default_config`, plus helper formatters (`apply_axes_format`, `apply_colorbar_format`, `apply_text_format`) used by V5 scatter blocks. | Revert `examples/visualize_dataset_metrics.m` from git history to restore inline hardcoded V5 formatting. |
| 2026-06-16 | Update | `v5 mean colorbar discrete ticks + turbo` | Updated V5 mean-mode side-ratio color encoding to use `turbo` and configured colorbar ticks/labels to match the discrete side-ratio values actually present in each subplot, improving interpretability for discrete ratio sets. | Updated `examples/visualize_dataset_metrics.m` mean-mode scatter branch in `plot_nobs_vs_denoising_scatter` to set `colormap(gca, turbo)`, derive unique finite side-ratio ticks from plotted data, and assign `cb.Ticks`/`cb.TickLabels`. | Revert `examples/visualize_dataset_metrics.m` from git history to restore prior colormap and continuous/default colorbar tick behavior. |
