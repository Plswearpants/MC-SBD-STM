# Real-data reference-slice updates

| Date | Type | Component | Change | Caller Migration | Legacy Recovery |
| --- | --- | --- | --- | --- | --- |
| 2026-08-20 | Update | `reference-slice normalize` | Dropped the second interactive `normalizeBackgroundToZeroMean3D` on `Y_ref` after the volume is already normalized. `Y_ref` is taken from the normalized stack and only `proj2oblique` is reapplied. | `scripts/real/MTSBD_block_realdata1.m`, `lib/wrapper/decomposeRefSliceReal.m` | Restore the second `normalizeBackgroundToZeroMean3D(Y_ref, 'dynamic')` call from git history. |
| 2026-08-20 | Update | `eta from saved noise ROI` | Reuse the background rectangle from volume normalize for `eta` / `eta_data3d` instead of a second `estimate_noise` pick. `estimate_noise3D` now accepts an optional position. | `scripts/real/MTSBD_block_realdata1.m`, `lib/wrapper/decomposeRefSliceReal.m`, `lib/estimate_noise3D.m` | Call `estimate_noise3D(Y, 'std')` without a position, or restore `estimate_noise(Y_ref, 'std')` from git history. |
| 2026-08-24 | Update | `first-block Y entry picker` | Replaced index-by-terminal input loading with interactive `.mat` selection (`uigetfile`) + variable entry picker (`listdlg`) when `Y` is absent. Defaults to entry `Y` if present, otherwise first variable; validates numeric matrix/stack before continuing. | `scripts/real/MTSBD_block_realdata1.m` | Restore the old `MT_SBD_ALL_INPUTS_DIR` + numeric file-index path from git history if launcher-only flow is preferred. |
| 2026-08-24 | Retirement | `auto-isolation center selection (real block)` | Removed the "most isolated points" branch from the kernel-center block and retired its logic in-place. The workflow now defaults to manual defect-center picking (`ginput`) for all kernels without a mode prompt. | `scripts/real/MTSBD_block_realdata1.m` | Restore the prior `choice` branch (`auto` vs `manual`) from git history if auto-isolation experiments are needed again. |
| 2026-08-24 | Update | `center source choice (default ref)` | Added two center-source choices in the all-slice kernel-center block: default to centers from reference-kernel initialization, or manually reselect via `ginput`. Extended `initialize_kernels` with optional third output `kernel_centers` to persist selected reference centers for reuse. | `scripts/real/MTSBD_block_realdata1.m`; `lib/initialize_kernels.m` | Restore manual-only center selection block and 2-output `initialize_kernels` call from git history. |
| 2026-08-24 | Update | `kernel-order preview UI` | Added a pre-selection visualization panel that plots all reference initialized kernels as ordered `K1...Kn` and overlays corresponding center labels on `Y_ref`, so kernel-center selection preserves order consistency. | `scripts/real/MTSBD_block_realdata1.m` | Remove the `Reference Kernel Order Preview` block from git history if this extra UI is not desired. |
| 2026-08-24 | Update | `trusted-slice default off` | Marked trusted-slice weighting as alpha and set unweighted mode as explicit default (`use_trusted_weights = false`), with manual trusted-slice presets also off by default. | `scripts/real/MTSBD_block_realdata1.m` | Re-enable trusted weighting defaults by restoring prior flags from git history when alpha experimentation resumes. |
| 2026-08-24 | Update | `real block numbering` | Numbered all active `%%` sections as `Block 01...27` and all retired sections as `R01...R25` to make execution order and legacy context explicit during refinement. | `scripts/real/MTSBD_block_realdata1.m` | Restore prior unnumbered section headers from git history if free-form headings are preferred. |
| 2026-08-24 | Update | `collapsed real blocks 1-13` | Collapsed the first 13 active sections into four macro blocks: (1) run setup+loading, (2) reference-run preparation, (3) reference run+visualization, (4) block-run preparation. Converted former internal block boundaries into action comments, and renumbered subsequent active blocks from `Block 05...18` for continuity. | `scripts/real/MTSBD_block_realdata1.m` | Restore the previous fine-grained `Block 01...27` segmentation from git history. |
| 2026-08-24 | Update | `visualization block IDs` | Renamed all active sections after `Block 05` to visualization IDs `V01...V13` as requested, keeping execution content unchanged. | `scripts/real/MTSBD_block_realdata1.m` | Restore `Block 06...18` headers from git history if unified block numbering is preferred. |
| 2026-08-24 | Update | `visualization index starts at V00` | Shifted visualization IDs so `Convert Aout_ALL to cell format` is `V00`, with subsequent visualization sections renumbered `V01...V12`. | `scripts/real/MTSBD_block_realdata1.m` | Restore the `V01...V13` labels from git history if 1-based visualization indexing is preferred. |
| 2026-08-24 | Add | `run_real_block trunk` | Added `scripts/real/run_real_block.m`, a standardized trunk for the preprocessed-`Y` entry point following the `run_real_data.m` format: Section 0 repo-root discovery, `PJ01R` project + fresh log file, `LR01B`/`RS01A`/`IP01R`/`BR01A` wrapper blocks with PRESETS and `DO NOT EDIT BELOW` regions, a pre-run checkpoint, and visualization cells `VR00R...VR12R` that each append a log entry. The `R01...R25` retired blocks are not carried over. | New file; `scripts/real/MTSBD_block_realdata1.m` is left untouched as the legacy reference. | Delete `scripts/real/run_real_block.m` and keep using `scripts/real/MTSBD_block_realdata1.m`. |
| 2026-08-24 | Add | `real-data logging` | Real-data wrappers now write log entries via the new `lib/basic/logBlockIfEnabled.m`, which no-ops when `log.path`/`log.file` are unset so `run_real_data.m` (which passes an empty `log`) is unaffected. `createProjectStructure` gained a `'prefix'` name-value pair so real runs land in `real_<timestamp>` instead of `synthetic_<timestamp>`. | `lib/wrapper/createProjectStructure.m`, `lib/wrapper/loadRealPreprocessed.m`, `lib/wrapper/decomposeRefSliceReal.m`, `lib/wrapper/initProliferationReal.m`, `lib/wrapper/runAllSlicesReal.m` | Remove `lib/basic/logBlockIfEnabled.m` and its call sites; drop the `'prefix'` parameter to restore the hardcoded `synthetic_` project name. |
| 2026-08-24 | Add | `loadRealPreprocessed` | New wrapper for the preprocessed-`Y` entry point: resolves the run-environment output root (`MT_SBD_RUN_ENV`), picks a `.mat` via `uigetfile`, picks the variable via `listdlg`, and validates it is a numeric stack. Both selections can be pinned with `cfg.load.preprocessed_file` / `cfg.load.preprocessed_var` for non-interactive replay. | New `lib/wrapper/loadRealPreprocessed.m`, called from `scripts/real/run_real_block.m`. | Delete the wrapper; Block 01 of `scripts/real/MTSBD_block_realdata1.m` still holds the inline version. |
| 2026-08-24 | Update | `real wrapper capabilities` | Ported block-script capabilities into the shared wrappers, all opt-in so `run_real_data.m` defaults are unchanged: `decomposeRefSliceReal` now captures `ref_kernel_centers` and reports the SNR diagnostic; `initProliferationReal` gained `cfg.blockInit.center_source` (`reference`/`manual`/`ask`), the `K1...Kn` order preview, and `reuse_noise_roi`; `runAllSlicesReal` gained kernel subsetting (`params.blockRun.kernels_to_run`), `xinit` warm start from `X_ref`, the slice/kernel-tagged output file, completion notification, and the observation-fidelity plot. Trusted-slice weighting now defaults off. | `lib/wrapper/decomposeRefSliceReal.m`, `lib/wrapper/initProliferationReal.m`, `lib/wrapper/runAllSlicesReal.m`, `config/init_config.m` | Restore the prior wrapper versions from git history; the corresponding inline logic remains in `scripts/real/MTSBD_block_realdata1.m`. |
| 2026-08-24 | Add | `run_real_preprocess trunk` | Added `scripts/real/run_real_preprocess.m`, the standardized trunk for the raw-to-`Y` half of the pipeline: Section 0 repo-root discovery, `PJ01P` project + fresh log, `LR01A` (`loadRealDataset`), `PR01A` (`preprocessRealData`), and `SV01P` which writes both a resumable checkpoint and a plain-variable `<run_label>_FULL.mat` handoff file. The `do_*` step toggles and `params.preprocessing.interactive` replay switch are exposed as presets. | New file; `scripts/real/preprocess.m` is left untouched as the legacy reference. | Delete `scripts/real/run_real_preprocess.m` and keep using `scripts/real/preprocess.m`. |
| 2026-08-24 | Update | `real trunk chaining` | `run_real_block.m` gained `params.project.existing_path`, which joins the project created by `run_real_preprocess.m` instead of creating a new one, so preprocessing and decomposition logs and artifacts share one project folder. `run_real_preprocess.m` prints the exact line to paste. Leaving it empty preserves the previous new-project behavior. | `scripts/real/run_real_block.m`, `scripts/real/run_real_preprocess.m` | Remove the `existing_path` branch from `PJ01R` to always create a new project. |
| 2026-08-24 | Update | `load/preprocess wrapper logging` | `loadRealDataset` and `preprocessRealData` now append `LR01A` / `PR01A` log entries via `logBlockIfEnabled` (recording source file, smoothing sigma, grid size, and which preprocessing steps actually ran). `loadRealDataset` also sets `meta.stage = "raw"`. Both remain no-ops for callers that pass an empty `log`, so `run_real_data.m` is unaffected. | `lib/wrapper/loadRealDataset.m`, `lib/wrapper/preprocessRealData.m` | Remove the `logBlockIfEnabled` calls and the `meta.stage` assignment from git history. |
| 2026-08-24 | Update | `visualization blocks unlogged` | Removed the `logUsedBlocks` calls from the `VR00R...VR12R` cells in the new trunk. Visualization is exploratory and re-runnable, so the log now records only the pipeline blocks (`PJ01R`, `LR01B`, `RS01A`, `IP01R`, pre-run checkpoint, `BR01A`) plus the closing `DONE` entry. | `scripts/real/run_real_block.m` | Restore the per-cell `logUsedBlocks(log.path, log.file, "VR..R", ...)` lines from git history. |
| 2026-08-24 | Update | `normalize ROI reuse` | All `normalizeBackgroundToZeroMean3D` calls in the preprocess trunk now pass and refresh `preprocessing_params.normalize_region`, so the background rectangle is drawn once and reused. After crop (`PR2.2`) the stored region is cleared because the FOV changed; the next normalize redraws on the cropped stack. The region is saved in the recipe with Y. | `scripts/real/run_real_preprocess.m` | Restore the 1-arg normalize calls (no position) from git history to re-prompt at every step. |
| 2026-08-24 | Update | `preprocessing kept step-by-step` | Replaced the single `PR01A` (`preprocessRealData`) call in the preprocess trunk with inline per-step blocks that keep the legacy `preprocess.m` numbering: `PR2.0`, `PR2.1` Bragg, `PR2.2` crop, `PR2.2b` slice selection (new, optional), `PR2.3` streak removal, `PR2.4` interpolation, `PR2.5` defect masking, `PR2.6a` streak correction, `PR2.6b` heal, `PR2.6c` directional plane, `PR2.end` final normalization. Preprocessing results decide whether later steps are wanted, so parameters are now set immediately before each step rather than all at once up front; each block previews `data_carried` and appends its own log entry, and `preprocessing_params` accumulates the recipe. `lib/wrapper/preprocessRealData.m` was reverted to its committed version and is still used by `run_real_data.m` for the whole-chain path. | `scripts/real/run_real_preprocess.m` | Restore the single `PR01A` block from git history to drive preprocessing through `preprocessRealData` in one call. |
| 2026-08-24 | Add | `promoted script helpers` | Promoted `notify_allslice_completion` (with its SMTP helpers) and `Xout_gaussian_broaden` from local functions in the block script into `lib/`, so wrappers and trunk cells can call them. The block script's local `build_auto_trusted_slice_weights` and `enforce_kernel_polarity` were verified behaviorally identical to the `lib/` versions, so the new trunk resolves to `lib/` with no change in results. | New `lib/notify_allslice_completion.m`, `lib/Xout_gaussian_broaden.m`; used by `lib/wrapper/runAllSlicesReal.m` and `scripts/real/run_real_block.m`. | Delete the promoted files; `scripts/real/MTSBD_block_realdata1.m` still defines all four locally. |
| 2026-08-25 | Retirement | `hist_ real trunks` | Renamed previous real trunks in-folder: `MTSBD_block_realdata1` → `hist_MTSBD_block_realdata1`, `preprocess` → `hist_preprocess`, combined `run_real_data` → `hist_run_real_data`, restored `postprocessing` as `hist_postprocessing`. Official split is `RUN_real_preprocess` + `RUN_real_block`. | `scripts/real/*`, root stubs, run-env launchers, `docs/REPO_STRUCTURE.md` | Run the `hist_*` files directly. |
