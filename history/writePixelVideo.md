# writePixelVideo

| Date | Type | Component | Change | Caller Migration | Legacy Recovery |
| --- | --- | --- | --- | --- | --- |
| 2026-08-13 | Add | `writePixelVideo` | Added pixel-to-pixel stack video writer: 1:1 array pixels, 10 px margins, energy stamp from `V`, no figure/`getframe` resampling. | New `lib/writePixelVideo.m`. Call as `writePixelVideo(data, V)` or `writePixelVideo(data, V, 'out.mp4')`. Existing `gridVideoWriter` callers are unchanged. | Remove `lib/writePixelVideo.m` or restore from git history. |
| 2026-08-13 | Update | `writePixelVideo` | Added static per-panel captions: `PanelGrid` (e.g. `[1 5]`), `PanelAnchor` (`bl`/`tl`/`tr`/`br`), `PanelLabels`, and caption block height `PanelLabelHeight` (default 0.2 of panel height). | Call `writePixelVideo(data, V, [1 5], 'bl', labels)` or the matching name-value arguments. Energy stamp is unchanged. | Restore prior `lib/writePixelVideo.m` from git history to drop panel captions. |
| 2026-08-13 | Update | `writePixelVideo` | Replaced 8x8 bitmap glyphs with Arial text (`Interpreter` none so labels like `Zr_2` stay literal) and set default `PanelLabelHeight` to 0.1. | Same call pattern; override with `'PanelLabelHeight', 0.2` to restore the previous size. | Restore previous `lib/writePixelVideo.m` from git history for bitmap glyphs and 20% height. |
| 2026-08-13 | Update | `writePixelVideo` | Panel labels with `_` (`Zr_2`, `Si_1h`) now draw a smaller, lowered Arial suffix instead of a literal underscore or TeX math font. | Same `{'Zr_2','Si_1h',...}` strings; optional `Zr_{2}` also works. | Restore prior renderer from git history for underscore-as-character labels. |
| 2026-08-13 | Fix | `writePixelVideo` | Subscript suffix was placed as a superscript because pixel y increases upward. Offset now moves the suffix down. | Re-run the same `writePixelVideo` call; no caller argument changes. | Restore the previous `+ 0.28*fs` offset from git history. |
| 2026-08-13 | Update | `writePixelVideo` | Frame now keeps the data/panel aspect ratio (`MatchAspect`, default true) instead of adding a uniform margin that distorted a 1-by-5 grid; data is centered and `Margin` becomes a per-side minimum. | Re-run the same call; pass `'MatchAspect', false` for the previous exact-10-px-on-all-sides framing. | Restore prior `lib/writePixelVideo.m` from git history for uniform margins. |
| 2026-08-13 | Fix | `writePixelVideo` | Captions were stretched and clipped on small panels (80x400 stacks): text width was clamped instead of scaled, and the energy stamp overflowed the 10 px top margin. Text is now rasterized large, cropped to its ink, and scaled with locked aspect; a header band (`EnergyLabelHeight`, default 0.12) is reserved above the data; panel captions have a 12 px floor. | Re-run the same call; tune with `'EnergyLabelHeight'` and `'PanelLabelHeight'`. | Restore prior `lib/writePixelVideo.m` from git history for the previous fixed-height text renderer. |
