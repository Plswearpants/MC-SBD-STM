## Update and Deprecation Log

| Date | Type | Component | Change | Caller Migration | Legacy Recovery |
| --- | --- | --- | --- | --- | --- |
| 2026-05-30 | Update | `plotting_paper block stitching` | Replaced hardcoded three-file loading and fixed slice offsets with folder-driven indexed selection plus strict manual-cutoff stitching. File slice ranges are parsed from `sXtoY` tokens, and invalid cutoffs now error instead of auto-falling back. | Updated `examples/data/ZrSiTe/result/plotting_paper.m` (`%% load chosen runs`, `%% Merge Aout_ALL from all blocks`) and added helpers `mergeAoutBlocksManualCutoff`, `parseSliceRangeFromFilename`, and MAT-only file listing in `chooseOrderedFilesFromFolder`. | Restore prior hardcoded filenames and offsets from git history for `examples/data/ZrSiTe/result/plotting_paper.m` if fixed-range stitching is needed. |
| 2026-05-30 | Update | `plotting_paper real-space outputs` | Added stitched real-space outputs to the result package so downstream analysis can use both FT-space and real-space data. Real-space `Y_used` is now loaded per block and stitched with the same strict manual cutoffs used for `Aout_ALL`. | Updated `examples/data/ZrSiTe/result/plotting_paper.m` to load `Y_used`, return `Y_realspace` from `mergeAoutBlocksManualCutoff`, use it in QPI sizing/symmetrization, and store `result.Y_realspace` plus `result.A_realspace`. | Recover prior FT-only result behavior from git history for `examples/data/ZrSiTe/result/plotting_paper.m`. |
| 2026-05-30 | Update | `plotting_paper naming convention` | Standardized naming so unsuffixed `Y` and `Aout` represent real-space data, while FT-space variables/results use `_qpi` suffixes. | Updated `examples/data/ZrSiTe/result/plotting_paper.m` fields/variables: `result.Y`, `result.Aout`, `result.Y_qpi`, `result.Aout_qpi`, `result.comparison_qpi`, plus corresponding local variable renames and downstream references. | Restore prior mixed naming from git history for `examples/data/ZrSiTe/result/plotting_paper.m` if needed. |
| 2026-05-30 | Update | `plotting_paper observation handling` | Removed observation stitching; now only kernels (`Aout`) are stitched by manual cutoffs. Observation `Y` is treated as shared input and sourced once from existing workspace `Y`/`Y_used` (fallback to first loaded block if needed). | Updated `examples/data/ZrSiTe/result/plotting_paper.m` merge call signature and `mergeAoutBlocksManualCutoff` to stitch kernels only; added shared-`Y` resolution block after merge. | Recover prior stitched-observation behavior from git history for `examples/data/ZrSiTe/result/plotting_paper.m`. |
| 2026-06-15 | Update | `plotting_paper SI synthetic denoising section` | Implemented SI synthetic comparison block with two plot sets: denoising sigma-ratio heatmaps and per-kernel `sqrt(Nobs)` vs denoising scatter by side-length ratio. Scatter is restricted to side-length-ratio bins `<= 0.2233` and generated for SNR targets `[3, 5, 7]` (nearest grid points). | Updated `examples/data/ZrSiTe/result/plotting_paper.m` under `%% SI -- Compare the SNR before and after MCSBD_synthetic data`; uses `build_denoising_sigma_metrics` + `metrics2heat_by_snr_interpolated`, and per-kernel fields `Nobs_per_kernel_at_axis3` / `denoising_sigma_ratio_per_kernel`. | Revert the SI block in `examples/data/ZrSiTe/result/plotting_paper.m` from git history to restore placeholder-only behavior. |
| 2026-06-15 | Update | `plotting_paper SI side-ratio cutoff input` | Added user-input cutoff for SI side-length-ratio selection (default `0.2233`) and explicit verification output for selected subplot count/values. Includes a warning when cutoff `0.2233` does not yield 8 bins. | Updated `examples/data/ZrSiTe/result/plotting_paper.m` SI block to prompt cutoff at section start and print selected side-ratio bins before plotting. | Revert SI cutoff prompt/verification lines in `examples/data/ZrSiTe/result/plotting_paper.m` from git history to restore fixed cutoff behavior. |
| 2026-06-15 | Update | `plotting_paper SI conditional metric rebuild` | Changed SI denoising workflow to rebuild denoising metrics only when required metric fields are missing/empty, reusing existing `metrics` values otherwise. | Updated `examples/data/ZrSiTe/result/plotting_paper.m` SI block to guard `build_denoising_sigma_metrics` with field-availability checks. | Revert conditional guard in `examples/data/ZrSiTe/result/plotting_paper.m` from git history to force rebuild on every run. |
| 2026-06-15 | Update | `plotting_paper SI font and layout controls` | Added user-editable font size input (default large) for SI synthetic plots and switched subplot arrangement to integer-factor layout preference (e.g., 8 -> 2x4). | Updated `examples/data/ZrSiTe/result/plotting_paper.m` SI block to prompt `si_font_size`, apply font sizing to SI figures, and use helper `choose_integer_subplot_layout` for scatter subplot grids. | Revert SI font prompt/layout helper changes in `examples/data/ZrSiTe/result/plotting_paper.m` from git history to restore prior fixed formatting behavior. |
| 2026-06-15 | Update | `plotting_paper SI scatter styling` | Removed the dashed `y=1` reference line from the SI `sqrt(Nobs)` vs denoising scatter plots. | Updated `examples/data/ZrSiTe/result/plotting_paper.m` SI scatter block to drop the `yline(..., ''--'', ...)` call. | Re-add the reference line in `examples/data/ZrSiTe/result/plotting_paper.m` from git history if visual parity guide is needed again. |
| 2026-06-15 | Update | `plotting_paper SI denoising label update` | Updated SI scatter y-axis label to match normalized denoising metric definition `(\sigma_{in}-\sigma_{out})/\sigma_{in}`. | Updated `examples/data/ZrSiTe/result/plotting_paper.m` SI scatter block y-axis text. | Restore prior `\sigma_{in}/\sigma_{out}` label from git history if needed. |
| 2026-06-16 | Update | `plotting_paper SI denoising ratio label restore` | Restored SI scatter y-axis labeling to ratio denoising `\sigma_{in}/\sigma_{out}` and removed normalized-metric y-limit clamp to allow ratio values above 1. | Updated `examples/data/ZrSiTe/result/plotting_paper.m` SI scatter label and removed fixed `ylim([0,1])` in per-kernel scatter plotting. | Revert `examples/data/ZrSiTe/result/plotting_paper.m` from git history to restore normalized-metric label and fixed y-limit behavior. |
| 2026-06-16 | Update | `plotting_paper SI stacking-law scatter` | Updated SI scatter diagnostic to test stacking hypothesis directly by plotting `1/\sqrt{Nobs}` vs `\sigma_{out}` and overlaying dashed hypothesis lines `y=\sigma_{in}x` (slope from median `\sigma_{in}` in each subplot). | Updated `examples/data/ZrSiTe/result/plotting_paper.m` SI scatter block to use `denoising_sigma_out_per_kernel` and `denoising_sigma_in_per_kernel`, change axis labels/title text, and add hypothesis-line overlays. | Revert `examples/data/ZrSiTe/result/plotting_paper.m` from git history to restore prior side-ratio scatter semantics and denoising-ratio y-axis. |

