# Simple MCSBD example updates

| Date | Type | Component | Change | Caller Migration | Legacy Recovery |
| --- | --- | --- | --- | --- | --- |
| 2026-08-25 | Update | `simple_MCSBD_example freeze/load` | Example now freezes a `saveDataset`-compatible workspace (`log`, `data`, `params` + matching `*_LOGfile.txt`) under `examples/example_data/simple_mcsbd_2d/`. GD01A generation is kept. WS01A is the one-shot freeze (no overwrite unless flagged). LD01A headlessly reloads that file and opens a new session log. DS01A still applies the script's MCSBD parameter set. | `examples/simple_MCSBD_example.m`; `lib/wrapper/loadWorkspace.m` (`interactive`, `new_log_path`, `new_log_file`); `.gitignore` exceptions for the bundled `.mat`/log; `docs/REPO_STRUCTURE.md`, `docs/REPO_STRUCTURE_DAG.md` | Run GD01A then DS01A without LD01A to keep in-memory generation. Restore the generate-only example from git history. Official UI load remains `loadWorkspace` default (`interactive` true). |
| 2026-08-25 | Update | `simple_MCSBD_example no session log` | Removed project folders, `logUsedBlocks`, `registerTunableRun`, and `loadWorkspace` from the example. Frozen file is `data` + `params` only. Wrappers still receive an empty `log` struct; `logUsedBlocks` no-ops when path/file are empty. | `examples/simple_MCSBD_example.m`; `lib/basic/logUsedBlocks.m`; `.gitignore`; living docs | Restore the logged freeze/load path from git history. Official trunks still pass a real `log.path`/`log.file`. |
